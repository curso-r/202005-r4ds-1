# 1) Importar o arquivo para uma pasta de projeto do RStudio.
# 2) Filtrar só as capitais do Brasil (eliminar todas as outras cidades, já tenho a lista das capitais)
# 3) Transoformar zero (0) mortes ou casos em 0.5 (para não ter problema na transformação logarítmica), isso depois da transformação no log base 2 vai virar -1;
# 4) Criar uma coluna (vetor) com a área em Km2 de cada capital (já tenho uma lista com a área em Km2 de cada capital);
# 5) Criar uma coluna com a população de cada capital (já tenho uma lista com a população)
# 6) Criar uma coluna com a densidade populacional de cada capital população / área em Km2;
# 7) Criar uma coluna com log na base 2 da coluna de óbitosacumulados (obitosAcumulado;  já tem no arquivo original)
# 8) Fazer um gráfico onde contém a data a partir da primeira morte no Brasil, no eixo x e no eixo Y o log2 do número de mortes acumulada; 


